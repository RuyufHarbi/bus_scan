

import 'package:flutter/material.dart';
class DriverSchedulePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Driver Account')),
      body: Center(child: Text('Driver Account Page')),
    );
  }
}